sudo: required

language: rust

rust:
    - nightly

cache: cargo

services:
    - docker

script:
    - cargo build --verbose
    - cargo test --verbose
    - docker build -t todo-tree .
    - docker ps -a
